## 2024-03-06 - A11y Requirement: ARIA labels on Icon-only buttons
**Learning:** Icon-only interactive elements (like window controls, sidebar toggles, and theme swatches) must have explicit `aria-label` attributes for screen reader accessibility, as relying solely on `title` attributes is insufficient.
**Action:** Always verify icon-only interactive elements have an `aria-label`.