## 2024-03-01 - Icon-only buttons without ARIA labels
**Learning:** Many icon-only buttons (`.icon-btn`, window controls, theme buttons) rely on `title` attributes or surrounding context, but lack explicit `aria-label` attributes for screen readers.
**Action:** Add descriptive `aria-label` attributes to icon-only buttons to improve accessibility.
